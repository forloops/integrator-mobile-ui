<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:IntegratorMobile.UI.Controls;assembly=IntegratorMobile.UI"
             x:Class="IntegratorMobile.Views.Auth.UpdatePasswordPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource BackgroundSecondary}">
    
    <ScrollView>
        <VerticalStackLayout Padding="24"
                             Spacing="24"
                             VerticalOptions="Center">
            
            <!-- Back Button -->
            <HorizontalStackLayout>
                <Button Text="‹ Back"
                        TextColor="{StaticResource TextPrimary}"
                        BackgroundColor="Transparent"
                        FontFamily="InterSemiBold"
                        Clicked="OnBackClicked" />
            </HorizontalStackLayout>
            
            <!-- Header -->
            <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                <Label Text="Update Temporary Password"
                       Style="{StaticResource HeadingLarge}"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center" />
                <Label Text="Please enter a new password to use."
                       Style="{StaticResource BodyMedium}"
                       TextColor="{StaticResource TextSecondary}"
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
            
            <!-- Card -->
            <controls:AppCard CardPadding="24">
                <VerticalStackLayout Spacing="20">
                    
                    <!-- Alert (shown when result is available) -->
                    <Border x:Name="AlertBox"
                            IsVisible="False"
                            BackgroundColor="{StaticResource SuccessLight}"
                            Stroke="Transparent"
                            StrokeShape="RoundRectangle 8"
                            Padding="16">
                        <Label x:Name="AlertMessage"
                               FontFamily="InterMedium" />
                    </Border>
                    
                    <!-- New Password Input -->
                    <controls:AppInput x:Name="PasswordInput"
                                      Label="Password"
                                      Placeholder="Enter a new password"
                                      IsPassword="True"
                                      ReturnType="Next" />
                    
                    <!-- Confirm Password Input -->
                    <controls:AppInput x:Name="ConfirmPasswordInput"
                                      Label="Confirm Password"
                                      Placeholder="Confirm your new password"
                                      IsPassword="True"
                                      ReturnType="Go"
                                      Completed="OnConfirmPasswordCompleted" />
                    
                    <!-- Update Password Button -->
                    <controls:AppButton x:Name="UpdateButton"
                                       Text="UPDATE PASSWORD"
                                       Variant="Primary"
                                       IsBlock="True"
                                       Clicked="OnUpdateClicked" />
                    
                </VerticalStackLayout>
            </controls:AppCard>
            
        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>
