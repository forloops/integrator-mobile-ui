<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:IntegratorMobile.UI.Controls;assembly=IntegratorMobile.UI"
             x:Class="IntegratorMobile.Views.Profile.ProfilePage"
             Title="My Profile"
             BackgroundColor="{StaticResource BackgroundSecondary}">
    
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">
            
            <!-- Profile Header -->
            <controls:AppCard>
                <VerticalStackLayout Spacing="16" HorizontalOptions="Center">
                    <Border WidthRequest="100" HeightRequest="100"
                            BackgroundColor="{StaticResource Primary100}"
                            StrokeThickness="0"
                            StrokeShape="RoundRectangle 50"
                            HorizontalOptions="Center">
                        <Label Text="👤" FontSize="48" 
                               HorizontalOptions="Center" VerticalOptions="Center" />
                    </Border>
                    
                    <VerticalStackLayout HorizontalOptions="Center" Spacing="4">
                        <Label Text="John Smith"
                               Style="{StaticResource HeadingMedium}"
                               HorizontalOptions="Center" />
                        <Label Text="Service Tech"
                               Style="{StaticResource BodyMedium}"
                               TextColor="{StaticResource Primary600}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </controls:AppCard>
            
            <!-- Contact Info -->
            <Label Text="CONTACT INFORMATION" Style="{StaticResource Caption}" Margin="4,8,0,0" />
            
            <controls:AppCard>
                <VerticalStackLayout Spacing="16">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="EMAIL" Style="{StaticResource Caption}" />
                        <Label Text="john.smith@crowther.com" Style="{StaticResource BodyMedium}" />
                    </VerticalStackLayout>
                    
                    <BoxView HeightRequest="1" Color="{StaticResource BorderLight}" />
                    
                    <VerticalStackLayout Spacing="4">
                        <Label Text="PHONE" Style="{StaticResource Caption}" />
                        <Label Text="(555) 123-4567" Style="{StaticResource BodyMedium}" />
                    </VerticalStackLayout>
                    
                    <BoxView HeightRequest="1" Color="{StaticResource BorderLight}" />
                    
                    <VerticalStackLayout Spacing="4">
                        <Label Text="USERNAME" Style="{StaticResource Caption}" />
                        <Label Text="jsmith" Style="{StaticResource BodyMedium}" />
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </controls:AppCard>
            
            <!-- Company Info -->
            <Label Text="COMPANY" Style="{StaticResource Caption}" Margin="4,8,0,0" />
            
            <controls:AppCard>
                <VerticalStackLayout Spacing="4">
                    <Label Text="Crowther Roofing" Style="{StaticResource BodyLarge}" FontFamily="InterSemiBold" />
                    <Label Text="Company ID: crowther" Style="{StaticResource Caption}" />
                </VerticalStackLayout>
            </controls:AppCard>
            
        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>
